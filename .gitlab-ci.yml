pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir -p public
    - cp index.html public/
    - cp -r css public/
    - cp -r js public/
  artifacts:
    paths:
      - public
  only:
    - main
